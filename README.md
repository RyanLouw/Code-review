# Code Review

Local AI-assisted code review and CI-style build runner.

The project will expose a FastAPI service that can build and test .NET projects in an isolated workflow, with Docker and AI review support added incrementally.
